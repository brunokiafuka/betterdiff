{
  "name": "whodidit-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "A forensic diff viewer monorepo",
  "scripts": {
    "dev:desktop": "pnpm --filter desktop dev",
    "build:desktop": "pnpm --filter desktop build",
    "preview:desktop": "pnpm --filter desktop preview"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
